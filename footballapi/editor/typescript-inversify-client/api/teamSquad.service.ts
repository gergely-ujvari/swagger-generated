/**
 * Football API
 * API for accessing various kinds of football data, updated in real time.
 *
 * OpenAPI spec version: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */
/* tslint:disable:no-unused-variable member-ordering */

import { Observable } from 'rxjs/Observable';
import 'rxjs/add/operator/map';
import 'rxjs/add/operator/toPromise';
import IHttpClient from '../IHttpClient';
import { inject, injectable } from 'inversify';
import { IAPIConfiguration } from '../IAPIConfiguration';
import { Headers } from '../Headers';
import HttpResponse from '../HttpResponse';

import { V2TeamSquad } from '../model/v2TeamSquad';
import { V2TeamSquadInput } from '../model/v2TeamSquadInput';

import { COLLECTION_FORMATS }  from '../variables';



@injectable()
export class TeamSquadService {
    @inject('IAPIConfiguration') private APIConfiguration: IAPIConfiguration;
    @inject('IApiHttpClient') private httpClient: IHttpClient;



    /**
     * 
     * Get teams squad
     * @param id Unique identifier of the resource.
     * @param memberStatus 
     * @param languageCode Returns translated response based on the language code
     
     */
    public v2TeamsIdSquadGet(id: string, memberStatus?: 'ALL' | 'ACTIVE' | 'INACTIVE', languageCode?: string, observe?: 'body', headers?: Headers): Observable<V2TeamSquad>;
    public v2TeamsIdSquadGet(id: string, memberStatus?: 'ALL' | 'ACTIVE' | 'INACTIVE', languageCode?: string, observe?: 'response', headers?: Headers): Observable<HttpResponse<V2TeamSquad>>;
    public v2TeamsIdSquadGet(id: string, memberStatus?: 'ALL' | 'ACTIVE' | 'INACTIVE', languageCode?: string, observe: any = 'body', headers: Headers = {}): Observable<any> {
        if (!id){
            throw new Error('Required parameter id was null or undefined when calling v2TeamsIdSquadGet.');
        }

        let queryParameters: string[] = [];
        if (memberStatus !== undefined) {
            queryParameters.push('memberStatus='+encodeURIComponent(String(memberStatus)));
        }
        if (languageCode !== undefined) {
            queryParameters.push('languageCode='+encodeURIComponent(String(languageCode)));
        }

        // authentication (basicAuth) required
        if (this.APIConfiguration.username || this.APIConfiguration.password) {
            headers['Authorization'] = btoa(this.APIConfiguration.username + ':' + this.APIConfiguration.password);
        }
        headers['Accept'] = 'application/json; charset=utf-8';

        const response: Observable<HttpResponse<V2TeamSquad>> = this.httpClient.get(`${this.APIConfiguration.basePath}/v2/teams/${encodeURIComponent(String(id))}/squad?${queryParameters.join('&')}` as any, headers);
        if (observe === 'body') {
               return response.map(httpResponse => httpResponse.response);
        }
        return response;
    }


    /**
     * 
     * Update TeamSquad members
     * @param squadMembers TeamSquad members to update. Null value for a property means no update
     * @param id Unique identifier of the resource.
     
     */
    public v2TeamsIdSquadPatch(squadMembers: V2TeamSquadInput, id: string, observe?: 'body', headers?: Headers): Observable<V2TeamSquad>;
    public v2TeamsIdSquadPatch(squadMembers: V2TeamSquadInput, id: string, observe?: 'response', headers?: Headers): Observable<HttpResponse<V2TeamSquad>>;
    public v2TeamsIdSquadPatch(squadMembers: V2TeamSquadInput, id: string, observe: any = 'body', headers: Headers = {}): Observable<any> {
        if (!squadMembers){
            throw new Error('Required parameter squadMembers was null or undefined when calling v2TeamsIdSquadPatch.');
        }

        if (!id){
            throw new Error('Required parameter id was null or undefined when calling v2TeamsIdSquadPatch.');
        }

        // authentication (basicAuth) required
        if (this.APIConfiguration.username || this.APIConfiguration.password) {
            headers['Authorization'] = btoa(this.APIConfiguration.username + ':' + this.APIConfiguration.password);
        }
        headers['Accept'] = 'application/json';
        headers['Content-Type'] = 'application/json';

        const response: Observable<HttpResponse<V2TeamSquad>> = this.httpClient.patch(`${this.APIConfiguration.basePath}/v2/teams/${encodeURIComponent(String(id))}/squad`, squadMembers as any, headers);
        if (observe === 'body') {
               return response.map(httpResponse => httpResponse.response);
        }
        return response;
    }

}
