/**
 * Football API
 * API for accessing various kinds of football data, updated in real time.
 *
 * OpenAPI spec version: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  EventTeamStats,
} from './models';

/**
 * eventTeamstatsGet - parameters interface
 */
export interface IEventTeamstatsGetParams {
  acceptLanguage?: string;
  lastUpdate?: Date;
}

/**
 * eventsIdTeamstatsGet - parameters interface
 */
export interface IEventsIdTeamstatsGetParams {
  id: number;
  acceptLanguage?: string;
}

/**
 * TeamStatsApi - API class
 */
@autoinject()
export class TeamStatsApi extends Api {

  /**
   * Creates a new TeamStatsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Get statistics for the performance of each team involved in the event
   * @param params.acceptLanguage List of acceptable human languages for response. https://en.wikipedia.org/wiki/Content_negotiation
   * @param params.lastUpdate Filter events, have been updated after the specified date-time. By default will return updates modified in the last 24 hours
   */
  async eventTeamstatsGet(params: IEventTeamstatsGetParams): Promise<Array<EventTeamStats>> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/event_teamstats`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'last_update': params['lastUpdate'],
      })
      .withHeader('Accept-Language', params['acceptLanguage'])
      // Authentication 'basicAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get statistics for the performance of each team involved in the event
   * @param params.id Event.id for which team stats are requested
   * @param params.acceptLanguage List of acceptable human languages for response. https://en.wikipedia.org/wiki/Content_negotiation
   */
  async eventsIdTeamstatsGet(params: IEventsIdTeamstatsGetParams): Promise<Array<EventTeamStats>> {
    // Verify required parameters are set
    this.ensureParamIsSet('eventsIdTeamstatsGet', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/events/{id}/teamstats`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      .withHeader('Accept-Language', params['acceptLanguage'])
      // Authentication 'basicAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

