/**
 * Football API
 * API for accessing various kinds of football data, updated in real time.
 *
 * OpenAPI spec version: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  V2Venues,
  V2VenuesGetId,
  V2PostVenues,
  V2VenuesPut,
  V2PutVenues,
  V2VenuesList,
} from './models';

/**
 * v2VenuesGet - parameters interface
 */
export interface IV2VenuesGetParams {
  offset: number;
  limit: number;
  seasonIds?: string;
  languageCode?: string;
}

/**
 * v2VenuesIdGet - parameters interface
 */
export interface IV2VenuesIdGetParams {
  id: string;
  languageCode?: string;
}

/**
 * v2VenuesIdPut - parameters interface
 */
export interface IV2VenuesIdPutParams {
  id: string;
  venue?: V2PutVenues;
}

/**
 * v2VenuesPost - parameters interface
 */
export interface IV2VenuesPostParams {
  venue?: V2PostVenues;
}

/**
 * VenuesApi - API class
 */
@autoinject()
export class VenuesApi extends Api {

  /**
   * Creates a new VenuesApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * List venues with offset and limit parameters
   * @param params.offset The number of items to skip before starting to collect the result set
   * @param params.limit Limit the number of results returned
   * @param params.seasonIds Filter venues which have hosted matches in the specified CSV season_ids
   * @param params.languageCode Returns translated response based on the language code
   */
  async v2VenuesGet(params: IV2VenuesGetParams): Promise<V2VenuesList> {
    // Verify required parameters are set
    this.ensureParamIsSet('v2VenuesGet', params, 'offset');
    this.ensureParamIsSet('v2VenuesGet', params, 'limit');

    // Create URL to call
    const url = `${this.basePath}/v2/venues`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'offset': params['offset'],
        'limit': params['limit'],
        'season_ids': params['seasonIds'],
        'language_code': params['languageCode'],
      })

      // Authentication 'basicAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get venue by id
   * @param params.id Unique identifier of the resource.
   * @param params.languageCode Returns translated response based on the language code
   */
  async v2VenuesIdGet(params: IV2VenuesIdGetParams): Promise<V2VenuesGetId> {
    // Verify required parameters are set
    this.ensureParamIsSet('v2VenuesIdGet', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/v2/venues/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'language_code': params['languageCode'],
      })

      // Authentication 'basicAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update venue information
   * @param params.id Unique identifier of the resource.
   * @param params.venue Venue information to save in persistance
   */
  async v2VenuesIdPut(params: IV2VenuesIdPutParams): Promise<V2VenuesPut> {
    // Verify required parameters are set
    this.ensureParamIsSet('v2VenuesIdPut', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/v2/venues/{id}`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPut()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['venue'] || {}))

      // Authentication 'basicAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Store a newly created venue in storage
   * @param params.venue 
   */
  async v2VenuesPost(params: IV2VenuesPostParams): Promise<V2Venues> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/v2/venues`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPost()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['venue'] || {}))

      // Authentication 'basicAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

