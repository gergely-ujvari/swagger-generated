/**
 * Football API
 * API for accessing various kinds of football data, updated in real time.
 *
 * OpenAPI spec version: 0.0.1
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  V2TeamSquadInput,
  V2TeamSquad,
} from './models';

/**
 * v2TeamsIdSquadGet - parameters interface
 */
export interface IV2TeamsIdSquadGetParams {
  id: string;
  memberStatus?: 'ALL' | 'ACTIVE' | 'INACTIVE';
  languageCode?: string;
}

/**
 * v2TeamsIdSquadPatch - parameters interface
 */
export interface IV2TeamsIdSquadPatchParams {
  squadMembers: V2TeamSquadInput;
  id: string;
}

/**
 * TeamSquadApi - API class
 */
@autoinject()
export class TeamSquadApi extends Api {

  /**
   * Creates a new TeamSquadApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * Get teams squad
   * @param params.id Unique identifier of the resource.
   * @param params.memberStatus 
   * @param params.languageCode Returns translated response based on the language code
   */
  async v2TeamsIdSquadGet(params: IV2TeamsIdSquadGetParams): Promise<V2TeamSquad> {
    // Verify required parameters are set
    this.ensureParamIsSet('v2TeamsIdSquadGet', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/v2/teams/{id}/squad`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'memberStatus': params['memberStatus'],
        'language_code': params['languageCode'],
      })

      // Authentication 'basicAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Update TeamSquad members
   * @param params.squadMembers TeamSquad members to update. Null value for a property means no update
   * @param params.id Unique identifier of the resource.
   */
  async v2TeamsIdSquadPatch(params: IV2TeamsIdSquadPatchParams): Promise<V2TeamSquad> {
    // Verify required parameters are set
    this.ensureParamIsSet('v2TeamsIdSquadPatch', params, 'squadMembers');
    this.ensureParamIsSet('v2TeamsIdSquadPatch', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/v2/teams/{id}/squad`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asPatch()
      // Encode body parameter
      .withHeader('content-type', 'application/json')
      .withContent(JSON.stringify(params['squadMembers'] || {}))

      // Authentication 'basicAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

